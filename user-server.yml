server:
  port: 8031
spring:
    config:
        activate:
            on-profile: default
management:
  security:
    enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:3000/eureka}
  instance:
    preferIpAddress: true
