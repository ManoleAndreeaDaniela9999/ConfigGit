server:
  port: 8282

spring:
    config:
        activate:
            on-profile: prod
            
value: 10
  
---

server:
  port: 8282
  
spring:
    config:
        activate:
            on-profile: prod
            
value: 20
   
---

server:
  port: 8282
  
spring:
    config:
        activate:
            on-profile: dev
            
value: 30
